import { defineConfig } from "@playwright/test";

export default defineConfig({
  // Configure Playwright aqui, se necessário.
  // Exemplo:
  // timeout: 60000,
  // use: {
  //   baseURL: "http://localhost:8080",
  // },
});
